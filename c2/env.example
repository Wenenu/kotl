# Server Configuration
PORT=3001

# JWT Secret Key - REQUIRED! No hardcoded fallback for security.
# Generate a new one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANT: Set this to a strong random string in production!
JWT_SECRET=your-strong-random-secret-key-here

# Default Admin Credentials (only used on FIRST RUN if database is empty)
# If not set, you must create the first user using: node manage-db.js create-user <username> <password>
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=changeme123

# NOWPayments Payment Integration (https://nowpayments.io) - OPTIONAL
# Sign up at https://nowpayments.io, get API key from dashboard
# Works globally, supports 200+ cryptocurrencies, automatic payments
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=  # Optional: for webhook signature verification

# Manual Payment Wallet Addresses (for direct crypto payments)
# Users will send crypto to these addresses and submit transaction hash for verification
# At least one wallet address is required if CryptoBot is not configured
WALLET_BTC=bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh
WALLET_ETH=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb
WALLET_USDT=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb

# Your panel URL (used for redirect after payment)
WEBPANEL_URL=https://your-panel-url.com

# Security Notes:
# - All passwords are hashed with bcrypt (includes automatic salting)
# - Each password gets a unique salt automatically
# - Change default password immediately after first login!
# - Use the database management script to manage users: node manage-db.js

