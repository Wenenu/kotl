# Server Configuration
PORT=3001

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a new one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=69b98337d00be3ceca424b4032f5cb86912be404e521287af185c7482178536f572858b39493b7a5a62d9f305dd3bc230683144444ae37a627a43133ba541a45

# Default Admin Credentials (only used on first run if no users.json exists)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin

# Note: After first run, users are stored in users.json
# You can change passwords via the API or by editing users.json (passwords are hashed)

